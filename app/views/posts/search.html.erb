<h2 class='text-muted'>
    <% if @post.any? %>
        <%= pluralize(@post.count, 'resultado')%> encontrados:
    <% else %>
        Nenhum resultado encontrado.
    <% end %>
</h2>

<% @posts.each do |post| %>
  <%= render post %>
<%end%> 